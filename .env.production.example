# ═══════════════════════════════════════════════════════════════════════════════
# Manchengo Smart ERP - Production Environment Variables
# Copy to .env and fill in all values before deploying
# ═══════════════════════════════════════════════════════════════════════════════

# Database
DB_PASSWORD=                          # openssl rand -base64 32

# JWT (CRITICAL - generate unique secrets, min 32 chars)
JWT_SECRET=                           # openssl rand -base64 64
JWT_REFRESH_SECRET=                   # openssl rand -base64 64
# JWT_SECRET_PREVIOUS=                # Set during secret rotation (N-1 acceptance, remove after 15min)

# QR Code Security
QR_SECRET_KEY=                        # openssl rand -base64 32

# Redis
REDIS_PASSWORD=                       # openssl rand -base64 32

# CORS & Cookies
CORS_ORIGINS=https://app.manchengo.dz
COOKIE_DOMAIN=.manchengo.dz
NEXT_PUBLIC_API_URL=https://api.manchengo.dz/api

# Sentry Error Monitoring
SENTRY_DSN=
SENTRY_DSN_FRONTEND=

# Security Alerting (optional)
SECURITY_ALERT_ENABLED=true
# SECURITY_WEBHOOK_URL=              # Slack/Telegram webhook for CRITICAL alerts

# Swagger (disabled in prod by default)
# SWAGGER_ENABLED=true               # Uncomment to enable /docs in production

# SSL (set after certbot setup)
SSL_CERT_PATH=/etc/letsencrypt/live/manchengo.dz/fullchain.pem
SSL_KEY_PATH=/etc/letsencrypt/live/manchengo.dz/privkey.pem
